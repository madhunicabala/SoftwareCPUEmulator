/* assembler.c — TODO: Phase 2
   Converts .asm source files into .bin machine code
   using the encoding defined in include/isa.h */
#include <stdio.h>
int main(void) {
    printf("Assembler: coming in Phase 2\n");
    return 0;
}
